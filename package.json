{
  "name": "gym-vault",
  "version": "0.11.2",
  "description": "Training dashboard, plans, goals and history as plain files in your Obsidian vault.",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "node scripts/run-tests.mjs",
    "preview": "node _preview/build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  },
  "engines": {
    "node": ">=18"
  }
}
